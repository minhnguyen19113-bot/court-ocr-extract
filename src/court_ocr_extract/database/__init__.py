"""Persistence foundation for the web platform.

The package intentionally performs no eager model import or database connection so
the existing OCR/extraction runtime remains independent from web dependencies.
"""

