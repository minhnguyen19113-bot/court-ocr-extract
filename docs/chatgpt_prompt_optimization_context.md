# Prompt Optimization Context

Current priority:

- Validate quality only on real court PDFs provided and run by the user.
- Do not use fake data to judge OCR/extraction quality.
- Unit tests may use minimal non-real contract fixtures for schema/control-flow only.
- Codex must not inspect real PDFs or derived real-data artifacts.
- Final deliverable is Excel, with visual QA and extraction preview before Excel acceptance.

See `README.md` for the active workflow and command list.
